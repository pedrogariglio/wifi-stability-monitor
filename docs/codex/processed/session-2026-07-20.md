# Sesion Codex - 2026-07-20

## Contexto

- Proyecto: `wifi-stability-monitor`
- Objetivo: iniciar Etapa 2 (Grafana) sin mezclar cambios de datasource ni dashboards.

---

## Diagnostico inicial

- Etapa 1 ya estaba cerrada con Prometheus + Node Exporter operativos.
- Healthchecks de `node-exporter` y `prometheus` ya estaban implementados.
- Faltaba comenzar Etapa 2 con una base reproducible en Compose.

---

## Cambios aplicados

### Infraestructura

1. Se agregó servicio `grafana` en `docker-compose.yml`:
   - Imagen fija: `grafana/grafana:11.0.0`
   - Container: `wifi-grafana`
   - Puerto: `3000:3000`
   - Red: `monitor-net`
   - Dependencia: `prometheus`
   - Política: `restart: unless-stopped`

2. Se agregó volumen nombrado:
   - `grafana-data:/var/lib/grafana`

### Documentacion

- `TODO.md`:
  - Etapa 2 actualizada como iniciada (`grafana` agregado, versión fijada, volumen persistente y exposición por puerto).
- `README.md`:
  - Se agregó acceso de Grafana y se aclaró que backend API es interno.
  - Etapa 2 pasó a estado parcial.
- `docs/PROJECT_STATUS.md`:
  - Se incorporó Grafana en la tabla de servicios.
  - Se actualizaron pendientes/completados de Etapa 1 (PromQL + healthchecks ya cerrados).
  - Se reflejó que Etapa 2 continúa con datasource y dashboards.
- `docs/deployment.md` y `docs/runbook.md`:
  - Se agregaron verificaciones para Grafana en `:3000`.
- `docs/codex/insights/docker.md`:
  - Se agregó recomendación reusable sobre fijar versión + volumen al crear servicios nuevos.

---

## Estado al cierre

- Grafana ya forma parte del stack en modo base (servicio + persistencia + acceso).
- Verificacion runtime ejecutada:
  - `docker compose up -d grafana`
  - `docker compose ps grafana` -> `Up`
  - `curl -i http://localhost:3000/` -> `302` (redireccion a login, esperado)
- Pendiente inmediato de Etapa 2:
  1. Configurar datasource Prometheus en Grafana.
  2. Crear primer dashboard de métricas del OS.
